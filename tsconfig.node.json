{
  "compilerOptions": {
    "target": "ES2022",
    // keep the lib simple – we only need basic ES features for the config file
    "lib": ["ESNext"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,

    // disable automatic `@types/*` inclusion; it was picking up node/estree/etc
    "types": []
  },
  "include": ["vite.config.ts"]
}
